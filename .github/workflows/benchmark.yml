name: Benchmark

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
      - 'src/veyra/benchmarks/**'
  pull_request:
    paths:
      - 'src/veyra/benchmarks/**'

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
        
    - name: Run Benchmarks
      run: |
        # Placeholder for actual benchmark command
        echo "Running benchmarks..."
        # python scripts/run_benchmark.py --config configs/benchmarks/standard.yaml
